
fn int putchar(int) @foreign;

fn int main()
{
    char character = 'a';

    while (character <= 'z')
    {
        putchar(int(character));
     
        character = character + char(1);
    }

    return 0;
}